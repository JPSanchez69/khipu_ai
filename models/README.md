# Modelo local

Copia aquí `gemma-3n-E2B-it-int4.litertlm`. El archivo no se versiona porque
ocupa varios GB. `run_web.cmd` lo sirve solo en `127.0.0.1` para que Chrome
pueda instalarlo en su almacenamiento local.
